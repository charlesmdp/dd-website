(()=>{'use strict';const menu=document.querySelector('.site-menu'),nav=document.querySelector('.site-nav');menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',open);nav.classList.toggle('is-open',open)});nav?.addEventListener('click',e=>{if(e.target.closest('a')){nav.classList.remove('is-open');menu?.setAttribute('aria-expanded','false')}});document.addEventListener('keydown',e=>{if(e.key==='Escape'){nav?.classList.remove('is-open');menu?.setAttribute('aria-expanded','false')}});const search=document.querySelector('#article-search'),category=document.querySelector('#article-category');function filter(){let n=0;document.querySelectorAll('.site-blog-card[data-search]').forEach(c=>{c.hidden=!(c.dataset.search.includes((search?.value||'').toLowerCase())&&(!category?.value||c.dataset.category===category.value));if(!c.hidden)n++});const status=document.querySelector('#search-status');if(status)status.textContent=n+(n===1?' article':' articles');const empty=document.querySelector('#search-empty');if(empty)empty.hidden=n>0}search?.addEventListener('input',filter);category?.addEventListener('change',filter);const progress=document.querySelector('.reading-progress');if(progress){const update=()=>{progress.style.width=Math.min(100,window.scrollY/Math.max(1,document.documentElement.scrollHeight-innerHeight)*100)+'%'};addEventListener('scroll',update,{passive:true});update()}})();
